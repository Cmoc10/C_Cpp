# CSC412 A2 Unix Programs

A collection of C programs covering file I/O, text processing, sorting, recursion, and related command-line utilities.

## Build

Compile each program with `gcc`; `fio.c` and `fio.h` provide file I/O support.
